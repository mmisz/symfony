{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_tag_create'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'title_tag_create'|trans }}</h1>
    {{ form_start(form, { method: 'POST', action: url('list_tag_create') }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action_save'|trans }}" class="btn btn-secondary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('list_tag_index') }}" class="btn btn-primary">
            {{ 'action_back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}
