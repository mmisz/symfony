{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_element_delete'|trans({'%id%': listElement.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title_element_delete'|trans({'%id%': listElement.id|default('')}) }}</h1>
    {{ form_start(form, { method: 'DELETE', action: url('list_element_delete', {id: listElement.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action_delete'|trans }}" class="btn btn-secondary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('to_do_show',{'id':listId}) }}" class="btn btn-primary">
            {{ 'action_back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}