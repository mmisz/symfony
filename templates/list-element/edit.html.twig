{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_element_edit'|trans({'%id%': listElement.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title_element_edit'|trans({'%id%': listElement.id|default('')}) }}</h1>
    {{ form_start(form, { method: 'PUT', action: url('list_element_edit', {id: listElement.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action_save'|trans }}" class="btn btn-secondary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('to_do_show',{'id':listId}) }}" class="btn btn-primary">
            {{ 'action_back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}
