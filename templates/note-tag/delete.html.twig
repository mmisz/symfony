{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_tag_edit'|trans({'%id%': noteTag.id|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title_tag_edit'|trans({'%id%': noteTag.id|default('')}) }}</h1>
    {{ form_start(form, { method: 'DELETE', action: url('note_tag_delete', {id: noteTag.id}) }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action_delete'|trans }}" class="btn btn-secondary" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('note_tag_index') }}" class="btn btn-primary">
            {{ 'action_back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}
